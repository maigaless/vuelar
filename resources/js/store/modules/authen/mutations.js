export const SET_USER = (state,data) =>{
    state.user = data
}

export const IS_AUTHENTICATED = (state,data) =>{
    state.is_Authenticated = data
}

export const IS_ADMIN = (state,data) =>{
    state.is_Admin = data
}