export const SET_CATEGROY = (state,data)=>{
    state.category = data
}

export const SET_CATEGRIES = (state,data)=>{
    state.categories = data
}

export const SET_PRODUCT = (state,data)=>{
    state.product = data
}

export const SET_PRODUCTS = (state,data)=>{
    state.products = data
}